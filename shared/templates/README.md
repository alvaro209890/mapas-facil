# shared/templates/

Templates `.mxd` operacionais (após preparação única descrita em F1-04).

| Arquivo | Status |
|---|---|
| [`MANIFEST.json`](MANIFEST.json) | `dinamica_retrato` → `Dinamica_retrato.mxd` (a preparar); demais stubs |
| `Dinamica_retrato.mxd` | **ausente** — gerado na preparação B1 no ArcMap |

Registro automatizado após preparação: `python ferramentas/registrar_template.py dinamica_retrato`.

Fonte bruta: [`../../Referencias_IMAP/MXD/`](../../Referencias_IMAP/MXD/).
Baseline PDF: [`../../Referencias_IMAP/Mapas/01/`](../../Referencias_IMAP/Mapas/01/).
